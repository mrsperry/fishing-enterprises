let workers = {
    initialize() {
        enterprises.workers_interval = window.setInterval(this.update, 15000);
    },

    load() {
        enterprises.current_view = "workers";
    },

    update() {

    },

    update_desk_display() {

    }
}